[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ibeat-dce"
version = "0.0.0"
dependencies = [
  'pandas',
  'miblab',
  'miblab_data',
  'tqdm',
]

[tool.setuptools.packages.find]
where = ["src"]